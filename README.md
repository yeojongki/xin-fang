## Installation

```bash
# first in root entry
$ npm i -g umi commitizen   # or yarn global add umi commitizen
$ npm i                     # or yarn

# then go any entry
$ yarn
```

## Running the app

```bash
# development mode
$ yarn server:dev          # server
$ yarn admin:dev           # admin
```
